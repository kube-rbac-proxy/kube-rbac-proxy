apiVersion: v1
kind: Service
metadata:
  labels:
    app: kube-rbac-proxy
  name: kube-rbac-proxy
spec:
  ports:
    - name: https
      port: 8443
      targetPort: https
    - name: proxy-endpoints
      port: 10443
      targetPort: 10443
  selector:
    app: kube-rbac-proxy
